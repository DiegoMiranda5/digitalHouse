let x, y;

x = 200;
y = 55.56;

console.log(
    `Suma\n` +
    `${x} + ${y} = ${x + y}
`);

console.log(
    `Resta\n` +
    `${x} - ${y} = ${x - y}
`);

console.log(
    `Multiplicacion\n` +
    `${x} * ${y} = ${x * y}
`);

console.log(
    `Division\n` +
    `${x} / ${y} = ${x / y}
`);

console.log(
    `Modulo\n` +
    `${x} % ${y} = ${x % y}
`);

console.log(
    `es Mayore\n` +
    `${x} > ${y} = ${x > y}
`);

console.log(
    `es Menor\n` +
    `${x} < ${y} = ${x < y}
`);